# My Chrome Extension — Entrega Intermediária

Extensão mínima (MV3) para **rodar E2E com Playwright em Docker** e **CI no GitHub Actions**.

## Como rodar local (sem Docker)
```bash
npm ci
npm run build
npm run test:e2e
